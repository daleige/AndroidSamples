<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chenyangqi.nested.scroll">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".demo3.Demo3Activity"></activity>
        <activity android:name=".demo2.Demo2Activity" />
        <activity android:name=".demo1.Demo1Activity" />
        <activity android:name=".IndexActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".nesting.NesstingScrollActivity" />
        <activity android:name=".old.NestedActivity" />
        <activity android:name=".coordinator.MainActivity" />
    </application>

</manifest>