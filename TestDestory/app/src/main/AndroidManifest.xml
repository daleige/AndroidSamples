<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chenyangqi.pdf.testdestory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestDestory">
        <activity
            android:name="com.chenyangqi.test.destorydemo.BActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.chenyangqi.test.destorydemo.AActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.chenyangqi.test.destorydemo.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>